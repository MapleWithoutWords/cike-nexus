[project]
name = "cike-nexus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Administrator", email = "Administrator@DESKTOP-95NRM61" }
]
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.19.1",
    "annotated>=0.0.2",
    "column>=0.4.3",
    "fastapi[standard]>=0.141.1",
    "langchain>=1.3.17",
    "langchain-openai>=1.6.0",
    "pymysql>=1.2.0",
    "sqlmodel>=0.0.39",
]

[project.scripts]
cike-nexus = "cike_nexus:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
